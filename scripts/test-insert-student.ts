import { createClient } from '@supabase/supabase-js'; import * as dotenv from 'dotenv'; dotenv.config(); const supabase = createClient(process.env.NEXT_PUBLIC_SUPABASE_URL!, process.env.SUPABASE_SERVICE_ROLE_KEY!); async function run() { const { data, error } = await supabase.from('students').insert({ email: 'test@test.com', full_name: 'Test', phone: '123' }).select().single(); console.log('Data:', data); console.log('Error:', error); } run();
